# Engineering Standards

ChatGPT Work implements only approved EP specifications, preserves architecture boundaries, avoids duplicate logic, uses semantic accessible mobile-first UI, handles invalid and boundary inputs safely, minimizes dependencies, updates tests/docs in the same EP, never silently removes behavior, and reports only tests actually performed.

No coding begins before an EP specification is approved.
