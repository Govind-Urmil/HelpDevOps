import {describe,it,expect} from 'vitest';import {CAPABILITIES,INPUT_KINDS} from '../src/core/capabilities.js';import {createDetection,createResult,createSessionItem,createSendToAction} from '../src/core/contracts.js';
describe('platform contracts',()=>{it('uses only approved capability language',()=>{expect(CAPABILITIES).toHaveLength(10);expect(new Set(CAPABILITIES).size).toBe(10);expect(CAPABILITIES).toContain('PREFLIGHT')});it('preserves the future input domains',()=>{expect(INPUT_KINDS).toContain('jenkinsfile');expect(INPUT_KINDS).toContain('terraform-diagnostic');expect(INPUT_KINDS).toContain('kubernetes-failure')});it('records evidence without fake confidence',()=>{const value=createDetection({kind:'yaml',evidence:[{signal:'document marker'}],limitations:['schema not checked']});expect(value.deterministic).toBe(true);expect(value).not.toHaveProperty('confidence');expect(value.limitations).toHaveLength(1)});it('creates complete result coverage',()=>{expect(createResult({summary:'x',checked:['syntax'],notChecked:['runtime']})).toMatchObject({coverage:{checked:['syntax'],notChecked:['runtime']}})});it('defaults session content to memory-only',()=>{expect(createSessionItem({id:'1',label:'x',capability:'INSPECT',createdAt:'now'}).inputPolicy).toBe('memory-only')});it('preserves input in send-to contracts by default',()=>{expect(createSendToAction({targetId:'x',label:'Inspect',accepts:['yaml']}).preservesInput).toBe(true)})});
