import fs from 'node:fs';import path from 'node:path';import {tools} from '../src/config/tools.js';import {CAPABILITIES,INPUT_KINDS} from '../src/core/capabilities.js';import {validateToolDefinitions,validateResourcePack,validateDomainResource} from '../src/core/resource-validation.js';
const root=process.cwd(),read=file=>JSON.parse(fs.readFileSync(path.join(root,file),'utf8')),fileExists=file=>fs.existsSync(path.join(root,file));const errors=validateToolDefinitions(tools,{capabilities:CAPABILITIES,inputKinds:INPUT_KINDS,fileExists});const packs=['cron','structured-data','encoding-hash','ipv4-cidr','linux-permissions','git-reference','dockerfile','docker-compose','kubernetes-manifest'].map(tool=>[tool,`src/tools/${tool}/resources.json`,`src/tools/${tool}/examples.json`]);for(const [tool,resourceFile,examplesFile] of packs){const resource=read(resourceFile);errors.push(...validateResourcePack(resource,read(examplesFile),tool),...validateDomainResource(resource,tool))}if(errors.length)throw new Error(errors.join('\n'));console.log(`Resource validation passed: ${packs.length} packs, ${tools.length} tools, registry routes/analyzers/references verified.`);
