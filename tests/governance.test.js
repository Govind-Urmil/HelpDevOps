import {describe,it,expect} from 'vitest';import {isCurrentRoute,navigation} from '../src/config/site.js';import {securityPolicy} from '../src/config/security.js';
describe('shell governance',()=>{it('matches current and nested navigation routes',()=>{expect(isCurrentRoute('/tools/','/tools/')).toBe(true);expect(isCurrentRoute('/tools/yaml/','/tools/')).toBe(true);expect(isCurrentRoute('/guides/','/tools/')).toBe(false)});it('keeps About out of primary navigation',()=>{expect(navigation.map(item=>item.label)).not.toContain('About')});it('keeps HSTS disabled until deployment approval',()=>{expect(securityPolicy.hsts.enabled).toBe(false);expect(securityPolicy.hsts.includeSubDomains).toBe(false)})});
