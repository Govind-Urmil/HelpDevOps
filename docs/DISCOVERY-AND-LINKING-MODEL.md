# Discovery and Linking Model

Unified search indexes compact reviewed fields only. Ranking is deterministic: exact title, exact reviewed error, alias, title prefix, complete token match, then partial token match. Result type and match reason are visible. Generic queries must not be presented as diagnoses.

Relationships use stable IDs and produce crawlable links. Every reference needs inbound and outbound value; links to planned or draft resources are blocked.
