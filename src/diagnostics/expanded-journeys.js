const reviewedAt='2026-07-26',reviewDue='2026-10-26';
const defs=[
['linux','memory-pressure','Linux memory pressure or OOM kill',['linux out of memory','oom killed process'],['out of memory: killed process','oom-kill','cannot allocate memory'],'free -h && vmstat 1 5','Confirm available memory, swap activity, and whether pressure persists.','Reduce or restart only the confirmed memory consumer using the service runbook, or restore an approved resource limit.','Memory headroom remains stable and the service completes representative work.','Alert on sustained memory pressure and review workload limits.','linux-permissions','Linux kernel OOM documentation','https://docs.kernel.org/mm/oom.html'],
['linux','permission-denied','Linux permission denied',['linux permission denied','file access denied'],['permission denied','operation not permitted','eacces'],'namei -l <path> && getfacl -p <path>','Inspect every path component, ownership, mode bits, ACLs, and the failing identity.','Apply the smallest owner-approved ownership, ACL, or mode correction to the confirmed path.','The original identity can perform only the intended operation.','Manage access declaratively and test service identities during deployment.','linux-permissions','GNU Coreutils file permissions','https://www.gnu.org/software/coreutils/manual/html_node/File-permissions.html'],
['docker','build-failure','Docker image build failure',['docker build failed','image build error'],['failed to solve','executor failed running','dockerfile parse error'],'docker build --progress=plain --no-cache -t <temporary-tag> .','Capture the first failing stage, instruction, exit code, and context path.','Correct the scoped instruction, context inclusion, or pinned dependency and rebuild a temporary tag.','A clean build succeeds and the image passes its smoke check.','Pin inputs, keep contexts small, and test clean builds in CI.','dockerfile','Docker build documentation','https://docs.docker.com/build/'],
['docker','network-connectivity','Docker container network connectivity',['docker network issue','container cannot connect'],['network not found','no route to host docker','temporary failure in name resolution docker'],'docker inspect <container> --format "{{json .NetworkSettings.Networks}}"','Confirm network attachments, target name, address, and listening port.','Attach the service to the intended declared network or correct the target name and port in Compose.','A request from the source container reaches the intended service after recreation.','Declare networks and health checks; avoid ephemeral addresses.','docker-compose','Docker networking overview','https://docs.docker.com/engine/network/'],
['kubernetes','image-pull-backoff','Kubernetes ImagePullBackOff',['image pull backoff','kubernetes cannot pull image'],['imagepullbackoff','errimagepull','failed to pull image'],'kubectl describe pod <pod> -n <namespace>','Read the image event, exact reference, node, and service account.','Correct the immutable image reference or update the approved image-pull credential.','A new pod pulls the intended digest and reaches Ready.','Validate image existence and credentials; prefer immutable digests.','kubernetes-manifest','Kubernetes images documentation','https://kubernetes.io/docs/concepts/containers/images/'],
['kubernetes','oomkilled','Kubernetes container OOMKilled',['kubernetes oom killed','pod out of memory'],['oomkilled','reason: oomkilled','exit code 137'],'kubectl get pod <pod> -n <namespace> -o jsonpath="{.status.containerStatuses[*].lastState.terminated}"','Confirm container, reason, limit, restarts, and memory trend.','Address the confirmed leak or demand; change resources only in reviewed deployment configuration.','Fresh pods remain Ready under representative load.','Set evidence-based resources and alert on growth and restarts.','kubernetes-manifest','Kubernetes resource management','https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'],
['kubernetes','service-unreachable','Kubernetes Service unreachable',['kubernetes service not reachable','service has no endpoints'],['no endpoints available for service','connection refused kubernetes service','service unavailable kubernetes'],'kubectl get service,endpointslice -n <namespace> -l kubernetes.io/service-name=<service> -o wide','Confirm selectors, ready endpoints, targetPort, source, and policies.','Correct the smallest selector, port, readiness, or policy mismatch in version-controlled manifests.','Requests from the intended source reach ready endpoints.','Test selectors, ports, readiness, and allowed traffic before deployment.','kubernetes-manifest','Kubernetes Services documentation','https://kubernetes.io/docs/concepts/services-networking/service/'],
['terraform','init-provider-failure','Terraform init or provider installation failure',['terraform init failed','provider installation failed'],['failed to install provider','failed to query available provider packages','inconsistent dependency lock file'],'terraform version && terraform providers','Record platform, provider source, constraints, lock selection, and registry error.','Correct the source or constraint and regenerate locks only through the reviewed dependency workflow.','A clean init succeeds on every supported CI platform.','Pin supported ranges and review lock-file updates.','', 'Terraform provider requirements','https://developer.hashicorp.com/terraform/language/providers/requirements'],
['terraform','plan-drift','Terraform unexpected plan or drift',['terraform unexpected changes','terraform drift'],['objects have changed outside of terraform','forces replacement','plan does not match'],'terraform plan -detailed-exitcode -out=<review-plan>','Preserve the plan and identify configuration, refresh, or replacement changes.','Reconcile the confirmed drift or intent; apply only the reviewed saved plan.','A fresh plan reports no unexpected differences.','Restrict out-of-band changes and run scheduled speculative plans.','', 'Terraform plan documentation','https://developer.hashicorp.com/terraform/cli/commands/plan'],
['git','merge-conflict','Git merge or rebase conflict',['git merge conflict','rebase conflict'],['merge conflict in','conflict (content)','could not apply'],'git status --short --branch && git diff --name-only --diff-filter=U','List unmerged paths, current operation, branch, and integrated commits.','Resolve each marked path, run checks, stage reviewed resolutions, and continue the operation.','No unmerged paths remain and tests pass on the resulting history.','Integrate smaller changes frequently and protect ownership-sensitive files.','git-reference','Git merge documentation','https://git-scm.com/docs/git-merge'],
['jenkins','build-stuck','Jenkins build stuck or waiting',['jenkins build stuck','jenkins job waiting'],['waiting for next available executor','still waiting to schedule task','paused for input'],'Review the queue reason, assigned node, executor state, and current Pipeline step.','Capture queue reason, label, executor availability, stage, and elapsed time.','Correct the label or capacity issue, or abort and replay only under the job owner runbook.','A new run advances through the affected stage and finishes normally.','Monitor queue age, right-size executors, and add stage timeouts.','', 'Jenkins Pipeline documentation','https://www.jenkins.io/doc/book/pipeline/'],
['cron','duplicate-execution','Cron job runs twice or overlaps',['cron runs twice','duplicate cron job'],['cron job overlapping','duplicate scheduled execution','job already running cron'],'crontab -l && systemctl list-timers --all','Compare every scheduler, identity, host replica, time zone, and overlap window.','Remove only the confirmed duplicate or add an owner-approved non-overlap guard.','The next two windows produce exactly one successful execution each.','Keep one declarative schedule owner and log run identifiers.','cron','crontab specification','https://pubs.opengroup.org/onlinepubs/9799919799/utilities/crontab.html'],
['networking','connection-timeout','Network connection timeout',['connection timed out','network timeout'],['connection timed out','operation timed out','etimedout'],'traceroute <host> || tracepath <host>','Confirm resolved address, port, route progress, source network, and timeout.','Correct the confirmed route, allow rule, or service exposure through the owning change process.','Connections from the source complete within expected latency.','Monitor from representative sources and document paths and ports.','ipv4-cidr','IETF TCP specification','https://www.rfc-editor.org/rfc/rfc9293'],
['networking','tls-certificate','TLS certificate verification failure',['tls certificate error','ssl certificate verify failed'],['certificate verify failed','certificate has expired','hostname mismatch'],'openssl s_client -connect <host>:443 -servername <host> -showcerts </dev/null','Record hostname, validity, issuer chain, verification code, and client time.','Deploy the correct renewed certificate and complete chain through the approved process.','Independent clients validate hostname, chain, and expected expiry.','Alert before expiry and continuously test the served chain.','', 'Mozilla TLS guidance','https://infosec.mozilla.org/guidelines/web_security#tls'],
['bash','environment-path','Shell command not found or wrong executable',['command not found','wrong command version'],['command not found','no such file or directory bash','bad interpreter'],'printf "shell=%s\\npath=%s\\n" "$SHELL" "$PATH"; type -a <command>','Compare contexts, resolved executable, shebang, permissions, and PATH order.','Use an explicit supported executable or correct the environment in the owning service or job.','The original non-interactive context runs the intended version.','Use explicit automation environments and validate dependencies.','', 'Bash command search documentation','https://www.gnu.org/software/bash/manual/html_node/Command-Search-and-Execution.html'],
['yaml','parse-error','YAML parse or indentation error',['yaml parse error','invalid yaml indentation'],['mapping values are not allowed here','did not find expected key','found character that cannot start any token'],'Validate the exact file and preserve the reported line and column.','Inspect the first parser location, indentation, tabs, quoting, and boundaries.','Correct only the confirmed syntax defect, then run the product-specific validator.','The YAML parser and consuming product validation both succeed.','Use spaces, formatting, and schema checks in editor and CI.','structured-data','YAML 1.2.2 specification','https://yaml.org/spec/1.2.2/']];
const rollbackByJourney={
 'linux-memory-pressure':'Restore the previous service revision or reviewed memory limit. If a restart or limit change worsens availability, follow the service failover procedure and preserve the new OOM evidence.',
 'linux-permission-denied':'Restore the recorded owner, group, mode, and ACL on the affected path; then verify both the intended identity and an unrelated identity before reopening traffic.',
 'docker-build-failure':'Restore the previous Dockerfile, dependency lock, and build context from version control; retain or redeploy the last-known-good image digest if the replacement image fails its smoke check.',
 'docker-network-connectivity':'Restore the previous Compose or container network declaration and recreate only the affected services; verify the former network attachments and service reachability.',
 'kubernetes-image-pull-backoff':'Restore the previous reviewed workload image reference and imagePullSecret reference. If a credential was rotated, revoke the failed replacement rather than restoring an exposed secret.',
 'kubernetes-oomkilled':'Restore the previous reviewed workload revision and resource settings; if the former limit is unsafe, keep the workload scaled down or failed over while the owner reviews memory evidence.',
 'kubernetes-service-unreachable':'Restore the previous Service, workload, readiness, or NetworkPolicy manifest from version control and confirm EndpointSlices and source-to-service traffic return to the prior state.',
 'terraform-init-provider-failure':'Restore the reviewed provider constraints and `.terraform.lock.hcl` from version control; remove only the failed local initialization cache before repeating a clean initialization.',
 'terraform-plan-drift':'Do not assume an applied infrastructure change is reversible. Restore configuration from version control and use the provider-specific recovery or state-repair procedure for each changed resource.',
 'git-merge-conflict':'Before completion, abort the merge or rebase. After completion, restore the recorded pre-operation commit on a recovery branch and coordinate any shared-history correction.',
 'jenkins-build-stuck':'Restore the previous agent label, executor, timeout, or Pipeline definition; stop the replacement run if it creates duplicate or unsafe work.',
 'cron-duplicate-execution':'Restore the exact saved scheduler entries and remove the new overlap guard only after confirming it blocked legitimate work; verify two subsequent schedule windows.',
 'networking-connection-timeout':'Revert only the reviewed route, firewall, security-group, or exposure change through its owning system; verify the prior policy and unrelated traffic are restored.',
 'networking-tls-certificate':'Redeploy the recorded previous certificate and chain only if they remain valid and trusted; otherwise fail over or stop the rollout and escalate to the certificate owner.',
 'bash-environment-path':'Restore the previous service, job, or shell environment definition and executable path; remove any temporary PATH override before retesting the original non-interactive context.',
 'yaml-parse-error':'Restore the previous source-controlled YAML file, discard the invalid edit, and rerun both YAML parsing and the consuming product validator.'
};
const evidenceBranches={
 'network-connectivity':[
  {id:'network-mismatch',label:'Source and target are not attached to the same intended network',title:'Network attachment or alias mismatch',summary:'The inspected network maps do not share the intended user-defined network or target alias.',command:'docker inspect <source> <target> --format "{{json .NetworkSettings.Networks}}"',purpose:'Compare network names and aliases for both containers.'},
  {id:'dns-failure',label:'The target name does not resolve inside the source container',title:'Container DNS or service-name failure',summary:'Name lookup fails from the source even though the containers share the intended network.',command:'docker exec <source> getent hosts <target>',purpose:'Test the exact target name from the source container without changing state.'},
  {id:'port-refused',label:'The name resolves, but the target port refuses the connection',title:'Listener or target-port mismatch',summary:'Routing and name resolution work, but the application is not listening on the expected container port.',command:'docker exec <target> ss -lnt',purpose:'List listening TCP sockets inside the target when the image provides ss.'}
 ],
 'image-pull-backoff':[
  {id:'image-missing',label:'Events report manifest unknown or image not found',title:'Image reference is missing',summary:'The registry responded, but the requested repository, tag, or digest does not exist.',command:'kubectl get pod <pod> -n <namespace> -o jsonpath="{.spec.containers[*].image}"',purpose:'Record the exact image reference declared on the Pod.'},
  {id:'auth-denied',label:'Events report unauthorized, denied, or no basic auth credentials',title:'Registry authentication is failing',summary:'The image exists or registry is reachable, but the Pod identity lacks a working pull credential.',command:'kubectl get serviceaccount <service-account> -n <namespace> -o jsonpath="{.imagePullSecrets[*].name}"',purpose:'List referenced pull-secret names without reading secret values.'},
  {id:'transport-failure',label:'Events report x509, DNS, timeout, or connection failure',title:'Registry transport or trust failure',summary:'The node cannot establish a trusted network path to the registry.',command:'kubectl get pod <pod> -n <namespace> -o wide',purpose:'Identify the scheduled node so the platform owner can inspect node-side DNS, egress, and trust.'}
 ],
 'service-unreachable':[
  {id:'no-endpoints',label:'The EndpointSlice has no addresses',title:'Service selectors produce no endpoints',summary:'The Service selector does not currently select eligible Pods.',command:'kubectl get service <service> -n <namespace> -o jsonpath="{.spec.selector}"',purpose:'Compare the Service selector with workload labels.'},
  {id:'not-ready',label:'Endpoint addresses exist but are not ready',title:'Selected Pods are not ready',summary:'The selector matches Pods, but readiness keeps them out of normal Service traffic.',command:'kubectl get pods -n <namespace> -l <selector> -o wide',purpose:'Inspect readiness and placement for selected Pods.'},
  {id:'ready-unreachable',label:'Ready endpoints exist, but traffic still fails',title:'Port mapping or traffic policy remains',summary:'Discovery is healthy; compare port to targetPort and then review source-scoped NetworkPolicies.',command:'kubectl get service <service> -n <namespace> -o yaml && kubectl get networkpolicy -n <namespace>',purpose:'Inspect port mapping and namespace policy without changing resources.'}
 ],
 'init-provider-failure':[
  {id:'registry-unreachable',label:'The error is DNS, TLS, proxy, timeout, or registry discovery',title:'Provider registry path is failing',summary:'Terraform cannot reach or trust the provider registry through the current execution environment.',command:'terraform providers',purpose:'Record required provider sources before the network or proxy owner investigates.'},
  {id:'constraint-empty',label:'No provider version satisfies the configured constraints',title:'Provider constraints are incompatible',summary:'The combined module constraints have an empty compatible version range.',command:'terraform providers',purpose:'Show which modules contribute each provider requirement.'},
  {id:'lock-platform',label:'The lock file is inconsistent or lacks this platform checksum',title:'Dependency lock selection needs reviewed renewal',summary:'Configuration constraints and the committed lock selection or platform hashes disagree.',command:'terraform providers',purpose:'Record provider sources and constraints before proposing a lock-file update.',lockAction:true}
 ],
 'connection-timeout':[
  {id:'dns-address',label:'DNS fails or resolves to an unexpected address',title:'Name resolution or split-horizon routing is wrong',summary:'The source is attempting the wrong address or cannot resolve the destination.',command:'getent ahosts <host> || nslookup <host>',purpose:'Record the addresses returned in the original source context.'},
  {id:'route-stops',label:'The route stops before the destination network',title:'Routing or an intermediate filter is dropping traffic',summary:'Packets do not reach the destination network from this source path.',command:'traceroute <host> || tracepath <host>',purpose:'Identify the last responding path boundary without scanning ports.'},
  {id:'host-reached',label:'The host path resolves, but only the destination port times out',title:'Destination filtering, listener, or return path remains',summary:'Basic reachability exists; the destination port or response path needs owner review.',command:'nc -vz -w 5 <host> <port>',purpose:'Attempt one bounded TCP connection to the known service port.'}
 ],
 'tls-certificate':[
  {id:'validity',label:'The certificate is expired or not yet valid',title:'Certificate validity window is wrong',summary:'The served leaf certificate is outside its validity interval relative to the client clock.',command:'date -u && openssl s_client -connect <host>:443 -servername <host> </dev/null 2>/dev/null | openssl x509 -noout -dates',purpose:'Compare UTC client time with the served leaf validity dates.'},
  {id:'hostname',label:'Verification reports a hostname mismatch',title:'The certificate does not cover the requested name',summary:'The requested hostname is absent from the served certificate subject alternative names.',command:'openssl s_client -connect <host>:443 -servername <host> </dev/null 2>/dev/null | openssl x509 -noout -ext subjectAltName',purpose:'Inspect names on the certificate selected through SNI.'},
  {id:'chain',label:'Verification reports unable to get issuer or an incomplete chain',title:'The served trust chain is incomplete or untrusted',summary:'The server omitted an intermediate or the client trust store does not trust the chain.',command:'openssl s_client -connect <host>:443 -servername <host> -showcerts </dev/null',purpose:'Capture the served chain and verification code for the certificate owner.'}
 ]
};
const build=d=>{const [domain,slug,title,aliases,exactErrors,check,evidence,fix,verification,prevention,tool,refTitle,refUrl]=d,p=`${domain}-${slug}`,n=x=>`${p}-${x}`,summary=`Diagnose ${title.toLowerCase()} with read-only evidence before a scoped change.`,rollback=rollbackByJourney[p];return{id:`journey-${domain}-${slug}`,slug,domain,title,summary,aliases,exactErrors,status:'technical-review',entryNodeId:n('start'),reviewedAt,reviewDue,riskProfile:'read-only-first',compatibility:{technology:domain[0].toUpperCase()+domain.slice(1),appliesGenerally:true,reviewedAgainst:['Current official general guidance'],limitations:['Provider, distribution, version, and local policy details may differ.','HelpDevOps does not inspect or change a live environment.']},whatItDoesNotProve:['The symptom alone does not prove one root cause.','Omitted context can change the safe response.'],firstChecks:[check],relatedToolIds:tool?[tool]:[],references:[{id:n('official'),title:refTitle,url:refUrl,type:'official'}],examples:[{id:n('example'),label:aliases[0],input:exactErrors[0],fixtureType:'synthetic',containsRealEnvironmentData:false,source:'HelpDevOps-authored',reviewedAt}],nodes:[{id:n('start'),nodeKind:'question',title:'Confirm the symptom and scope',summary,why:'Scope prevents a familiar error from being treated as proof.',risk:'read-only',choices:[{id:'confirmed',label:'The symptom and affected scope are confirmed',nextNodeId:n('collect')},{id:'unknown',label:'The scope or ownership is unclear',nextNodeId:n('escalate')}]},{id:n('collect'),nodeKind:'check',title:'Collect the minimum useful evidence',summary:evidence,why:'The first read-only check separates common branches without changing state.',risk:'read-only',commands:[{command:check,purpose:evidence}],choices:[{id:'cause-found',label:'Evidence identifies a specific, owned cause',nextNodeId:n('decide')},{id:'unclear',label:'Evidence is incomplete or conflicting',nextNodeId:n('escalate')}]},{id:n('decide'),nodeKind:'question',title:'Is the change scoped and reversible?',summary:'Confirm ownership, expected impact, and recovery before modifying state.',risk:'read-only',choices:[{id:'safe',label:'The owner approved a reversible change',nextNodeId:n('fix')},{id:'unsafe',label:'Ownership, impact, or rollback is unclear',nextNodeId:n('escalate')}]},{id:n('fix'),nodeKind:'action',title:'Apply the reviewed correction',summary:fix,risk:'moderate-risk',prerequisites:['Preserve current evidence and configuration.','Confirm owner, change window, and impact.'],rollback,choices:[{id:'done',label:'The reviewed change completed',nextNodeId:n('verify')},{id:'unsafe',label:'The change failed or impact is unexpected',nextNodeId:n('escalate')}]},{id:n('verify'),nodeKind:'verification',title:'Verify the original behavior',summary:verification,risk:'read-only',verification:[verification,'No new warnings or regressions appear in the affected scope.'],choices:[{id:'resolved',label:'The original symptom is resolved',nextNodeId:n('complete')},{id:'not-resolved',label:'The symptom remains or recurs',nextNodeId:n('escalate')}]},{id:n('complete'),nodeKind:'completion',title:'Recovery verified',summary:'Record the evidence, change, verification, and owner follow-up.',risk:'read-only',prevention:[prevention,'Keep the verification in the service runbook or delivery checks.']},{id:n('escalate'),nodeKind:'escalation',title:'Escalate with preserved evidence',summary:'Stop changing state. Share the symptom, scope, evidence, attempted change, and rollback status with the owner.',risk:'read-only'}],path:`/troubleshoot/${domain}/${slug}/`}};
function specialize(journey){
 const specs=evidenceBranches[journey.slug];if(!specs)return journey;
 const ids=suffix=>`${journey.domain}-${journey.slug}-${suffix}`;
 const collect=journey.nodes.find(node=>node.id===ids('collect'));
 collect.summary='Choose the branch that matches the observed read-only evidence.';
 collect.choices=[...specs.map(spec=>({id:spec.id,label:spec.label,nextNodeId:ids(spec.id)})),{id:'unclear',label:'The evidence is incomplete or does not match these branches',nextNodeId:ids('escalate')}];
 const branchNodes=specs.map(spec=>({id:ids(spec.id),nodeKind:'interpretation',title:spec.title,summary:spec.summary,why:'This branch is selected from an observed error class or runtime relationship, not from a guessed root cause.',risk:'read-only',commands:[{command:spec.command,purpose:spec.purpose}],choices:[{id:'continue',label:'This evidence is confirmed; review the scoped correction',nextNodeId:spec.lockAction?ids('lock-review'):ids('decide')},{id:'unclear',label:'The follow-up evidence conflicts or remains unclear',nextNodeId:ids('escalate')}]}));
 if(specs.some(spec=>spec.lockAction))branchNodes.push(
  {id:ids('lock-review'),nodeKind:'question',title:'Is a dependency lock update reviewed and reversible?',summary:'The command can download provider metadata and modify `.terraform.lock.hcl`; review the target platforms, repository diff, and rollback before running it.',why:'A lock-file update changes a version-controlled dependency integrity record and is not a read-only diagnostic check.',risk:'read-only',choices:[{id:'safe',label:'The owner approved the platforms and lock-file review',nextNodeId:ids('lock-update')},{id:'unsafe',label:'Platforms, ownership, or rollback are unclear',nextNodeId:ids('escalate')}]},
  {id:ids('lock-update'),nodeKind:'action',title:'Generate the reviewed platform checksum update',summary:'Run the lock command only in the correct configuration directory, then inspect the exact lock-file diff before committing or using it.',why:'Terraform providers lock writes provider selections and checksums to the dependency lock file.',risk:'moderate-risk',commands:[{command:'terraform providers lock -platform=<os_arch>',purpose:'Update `.terraform.lock.hcl` for one explicitly reviewed target platform.'}],prerequisites:['Confirm the provider source, constraints, target platform, and working directory.','Preserve the current `.terraform.lock.hcl` in version control and verify registry trust policy.'],rollback:'Discard the unapproved `.terraform.lock.hcl` diff or restore the reviewed file from version control; remove only provider artifacts created by this attempted update.',choices:[{id:'done',label:'The lock-file diff is reviewed and approved',nextNodeId:ids('verify')},{id:'unsafe',label:'The command or diff is unexpected',nextNodeId:ids('escalate')}]}
 );
 const decideIndex=journey.nodes.findIndex(node=>node.id===ids('decide'));
 journey.nodes.splice(decideIndex,0,...branchNodes);
 return journey;
}
export const expandedJourneys=Object.freeze(defs.map(d=>specialize(build(d))));
