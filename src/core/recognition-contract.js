export const RESULT_TYPES=Object.freeze({EXACT_MATCH:'EXACT_MATCH',PARTIAL_MATCH:'PARTIAL_MATCH',AMBIGUOUS_MATCH:'AMBIGUOUS_MATCH',STRUCTURED_INPUT:'STRUCTURED_INPUT',MALFORMED_STRUCTURED_INPUT:'MALFORMED_STRUCTURED_INPUT',UNSUPPORTED:'UNSUPPORTED',INVALID_INPUT:'INVALID_INPUT',SENSITIVE_CONTENT:'SENSITIVE_CONTENT'});
export const VALIDATION_STATES=Object.freeze({VALID:'VALID',INVALID:'INVALID',UNSUPPORTED:'UNSUPPORTED',NOT_APPLICABLE:'NOT_APPLICABLE',UNKNOWN:'UNKNOWN'});
export const confidenceBand=score=>score>=90?'HIGH':score>=70?'MEDIUM':score>=40?'LOW':'INSUFFICIENT';
const groups=value=>({signature:[...(value?.signature||[])],technology:[...(value?.technology||[])],context:[...(value?.context||[])],structure:[...(value?.structure||[])],exclusions:[...(value?.exclusions||[])]});
export function candidate(value={}){const recognition=Math.max(0,Math.min(100,Number(value.recognitionConfidence??value.confidenceScore)||0)),intent=Math.max(0,Math.min(100,Number(value.intentConfidence??recognition)||0));return Object.freeze({recognizer:value.recognizer||'unknown',technologyId:value.technologyId||value.technology||'unknown',candidateType:value.candidateType||'operational',capability:value.capability||null,intentConfidence:intent,recognitionConfidence:recognition,confidenceScore:recognition,confidenceBand:value.confidenceBand||confidenceBand(recognition),validationState:value.validationState||VALIDATION_STATES.NOT_APPLICABLE,evidenceGroups:groups(value.evidenceGroups),evidence:value.evidence||[],matchedSignature:value.matchedSignature||null,missingContext:value.missingContext||[],structuredIntent:Boolean(value.structuredIntent),destination:value.destination||null,proposedResultType:value.proposedResultType||null,explanation:value.explanation||'',rejectionReason:value.rejectionReason||'',followUpQuestion:value.followUpQuestion||'Add the command, technology, resource type, or surrounding output.',result:value.result||null});}
const publicCandidate=value=>({recognizer:value.recognizer,technology:value.technologyId,technologyId:value.technologyId,capability:value.capability,candidateType:value.candidateType,confidenceScore:value.recognitionConfidence,confidenceBand:value.confidenceBand,matchedSignature:value.matchedSignature,missingContext:value.missingContext,destination:value.destination,explanation:value.explanation,rejectionReason:value.rejectionReason,followUpQuestion:value.followUpQuestion});
export function decorateResult(result,{resultType,candidate:chosen=null,alternatives=[],rejectedCandidates=[]}={}){return{...result,resultType,technology:chosen?.technologyId||'unknown',technologyId:chosen?.technologyId||'unknown',confidenceScore:chosen?.recognitionConfidence||0,confidenceBand:chosen?.confidenceBand||'INSUFFICIENT',validationState:chosen?.validationState||VALIDATION_STATES.UNKNOWN,matchedEvidence:chosen?.evidence||result.evidence||[],matchedSignature:chosen?.matchedSignature||null,missingContext:chosen?.missingContext||[],parser:chosen?.recognizer||'none',destination:chosen?.destination||null,explanation:chosen?.explanation||result.summary||'',followUpQuestion:chosen?.followUpQuestion||null,alternativeCandidates:alternatives.map(publicCandidate),rejectedCandidates:rejectedCandidates.map(publicCandidate)};}
